export interface Point {
  x: number;
  y: number;
}

export interface GraphConfig {
  width: number;
  height: number;
  xMin: number;
  xMax: number;
  yMin: number;
  yMax: number;
  gridSize: number;
}